# Installation


```
sudo apt-get install -y python-dev libxml2-dev libxslt1-dev antiword unrtf poppler-utils pstotext tesseract-ocr \
flac ffmpeg lame libmad0 libsox-fmt-mp3 sox libjpeg-dev swig python3-pip
pip3 install textract
sudo apt install ocrmypdf
pip3 install ocrmypdf

pip3 install pydrive

#pip3 install opencv-python
#brew install poppler

# use this command to install PIL
# pip install Pillow
```

